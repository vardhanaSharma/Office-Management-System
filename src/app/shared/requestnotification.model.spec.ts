import { Requestnotification } from './requestnotification.model';

describe('Requestnotification', () => {
  it('should create an instance', () => {
    expect(new Requestnotification()).toBeTruthy();
  });
});
